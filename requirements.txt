# 依赖安装(在项目目录下运行)
# pip install -r requirements.txt

langchain>=1.0.0
langchain-community>=0.3.0
langgraph>=0.2.0
dashscope>=1.20.0          # 千问官方 SDK(langchain_community 依赖)
flask==3.0.3
flask-cors==4.0.1
requests>=2.31.0           # HTTP 请求库(用于百度搜索 API)
